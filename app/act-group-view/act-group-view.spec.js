'use strict';

describe('parayer.actGroupView module', function() {

  beforeEach(module('parayer.actGroupView'));

  describe('actGroupView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var actGroupViewCtrl = $controller('actGroupViewCtrl');
      expect(actGroupViewCtrl).toBeDefined();
    }));

  });
});