'use strict';

describe('parayer.actAreaView module', function() {

  beforeEach(module('parayer.actAreaView'));

  describe('actAreaView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var actAreaViewCtrl = $controller('actAreaViewCtrl');
      expect(actAreaViewCtrl).toBeDefined();
    }));

  });
});