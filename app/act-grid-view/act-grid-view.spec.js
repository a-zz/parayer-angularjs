'use strict';

describe('parayer.actGridView module', function() {

  beforeEach(module('parayer.actGridView'));

  describe('actGridView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var actGridViewCtrl = $controller('actGridViewCtrl');
      expect(actGridViewCtrl).toBeDefined();
    }));

  });
});