'use strict';

describe('parayer.projectView module', function() {

  beforeEach(module('parayer.projectView'));

  describe('projectView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var projectViewCtrl = $controller('projectViewCtrl');
      expect(projectViewCtrl).toBeDefined();
    }));

  });
});